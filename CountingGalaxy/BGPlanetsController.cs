using UnityEngine;

namespace Activities.CountingGalaxy
{
    public class BGPlanetsController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
